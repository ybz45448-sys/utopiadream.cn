# 测试环境配置：连接独立的测试数据库，避免污染开发库
# 第50天改造：同样支持环境变量覆盖
# CI（持续集成）环境里可能用不同的数据库，用 TEST_DB_* 覆盖
spring.datasource.url=${TEST_DB_URL:jdbc:postgresql://localhost:5432/utopia_test}
spring.datasource.username=${TEST_DB_USERNAME:postgres}
spring.datasource.password=${TEST_DB_PASSWORD:123456}

